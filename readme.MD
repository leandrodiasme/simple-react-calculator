# Simple React Calculator

A simple and clean calculator interface built using **React 18** and **Tailwind CSS**, running entirely in the browser without any build tools or bundlers.

This project was created as a learning exercise to explore component composition, reusable UI patterns, and modern styling approaches.

---

## ✨ Features

- ⚛️ Built with React 18 (via CDN)
- 🎨 Styled with Tailwind CSS (browser version)
- 🧩 Reusable components (`Text`, `Button`, `Card`)
- 🔁 Variant-based component styling
- 🧠 Clean component composition
- 📱 Responsive layout
- 🚀 No build step required — just open the HTML file

---

## 🛠️ Technologies Used

- HTML5
- JavaScript (ES6+)
- React 18
- ReactDOM
- Tailwind CSS (via CDN)
- Babel (for JSX support in the browser)

---

## 📂 Project Structure

```txt
/
├── index.html
└── README.md
